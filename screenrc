shell -$SHELL

# Disable the startup message
startup_message off         #default: on

# Change escape key
escape ^AA                  #default: ^Aa (C-A) 

# Large scrollback buffer
defscrollback 5000          #default: 100

# Save layout on detach/re-attach
layout save default
